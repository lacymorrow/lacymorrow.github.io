# `php-text-image-gen`

A PHP script which converts a string into an image of any size or color, using any True-Type Font (`.ttf`) provided. Before web fonts were supported by most browsers, this was the script I used to generate headers in custom fonts.

## Readme

<Readme username='lacymorrow' repo='php-text-image-gen' />
